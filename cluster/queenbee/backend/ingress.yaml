---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: queenbee-backend-ingress
  namespace: queenbee
spec:
  ingressClassName: nginx-ntnu
  tls:
    - hosts:
        - dev.api.queenbee.login.no
  rules:
    - host: dev.api.queenbee.login.no
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: admin-api
                port:
                  number: 8080
