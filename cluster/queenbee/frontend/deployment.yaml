---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: queenbee-frontend
  name: queenbee-frontend
  namespace: queenbee
spec:
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: queenbee-frontend
  template:
    metadata:
      labels:
        app: queenbee-frontend
    spec:
      imagePullSecrets:
        - name: gitlab-frontend-pull-token
      containers:
        - image: registry.login.no/tekkom/web/beehive/queenbee:2.0.13-dev
          imagePullPolicy: Always
          name: queenbee-frontend
          env:
            - name: BASE_URL
              value: https://queenbee-api-dev.login.no/v1
            - name: TYPE
              value: "test"
            - name: PROJECT_ID
              value: "test"
            - name: PRIVATE_KEY_ID
              value: "test"
            - name: PRIVATE_KEY
              value: "test"
            - name: CLIENT_EMAIL
              value: "test"
            - name: CLIENT_ID
              value: "test"
            - name: AUTH_URI
              value: "test"
            - name: TOKEN_URI
              value: "test"
            - name: AUTH_CERT_URL
              value: "test"
            - name: CLIENT_CERT_URL
              value: "test"
            - name: UNIVERSE_DOMAIN
              value: "test"
          ports:
            - containerPort: 4200
              name: 4200tcp
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: 4200
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 4200
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - queenbee-frontend
                topologyKey: kubernetes.io/hostname
