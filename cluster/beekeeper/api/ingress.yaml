---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beekeeper-api
  namespace: beekeeper
spec:
  ingressClassName: nginx-ntnu
  tls:
    - hosts:
        - dev.api.beekeeper.login.no
  rules:
    - host: dev.api.beekeeper.login.no
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: beekeeper-api
                port:
                  number: 8080
